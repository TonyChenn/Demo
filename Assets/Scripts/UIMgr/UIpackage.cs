namespace SFramework.Runtime
{
    // UI层级
    public enum UILayer
    {
        Default,
        Pop,
        Top,
    }
    // UI包名
    public enum UIPackage
    {
        None,
        MainPkg,
    }
}